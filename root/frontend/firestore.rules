rules_version = '2';
service cloud.firestore {
    match /databases/{database}/documents {
        match /{document=**} {
            // только для тестового задания, в продакшене так нельзя
            allow read, write: if true;
        }
    }
}
